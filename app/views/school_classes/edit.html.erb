<% provide(:title, 'Update Class') %>
<h1>Update Class</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@school_class) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>