<% provide(:title, @school_class.title) %>

<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @school_class.title %>
      </h1>
        <% if !is_student? %>
          <%= link_to "Update Class", edit_school_class_path(@school_class) %>
          <br>
        <% else %>
        <% end %>
    </section>
  </aside>
  <div>
    <%= @school_class.description %>
  </div>
</div>